<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.backend_mini.mapper.UserRoleMapper">
    <resultMap id="UserRoleMap" type="com.korit.backend_mini.entity.UserRole">
        <id property="userRoleId" column="user_role_id"/>
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
        <result property="createDt" column="create_dt"/>
        <result property="updateDt" column="update_dt"/>
    </resultMap>
    <insert id="addUserRole" useGeneratedKeys="true" keyProperty="userRoleId">
        insert into
            user_role_tb (user_id, role_id, create_dt, update_dt)
        values (#{userId}, #{roleId}, now(), null);
    </insert>
    <update id="modifyUserRole">
        update user_role_tb
        set role_id = #{roleId}
        where user_id = #{userId};
    </update>
</mapper>